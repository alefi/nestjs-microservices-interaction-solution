import { GameServiceV1 } from '@lib/grpc';

// eslint-disable-next-line @typescript-eslint/no-unused-vars
export const checkGameBidConstraints = (_applyBidParamsDto: GameServiceV1.ApplyBidParamsDto) => {
  // Here could be some validations
};
